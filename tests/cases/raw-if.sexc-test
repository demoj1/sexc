(%def-fn int sign ((int x))
  (%block
    (%if (< x 0)
      (%return -1)
      (%if (> x 0)
        (%return 1)
        (%return 0)))))
;==EXPECTED==
int sign(int x)
{
  if (x < 0)
    return -1;
  else
    if (x > 0)
      return 1;
    else
      return 0;
}
