(include "stdio.h")
(defn void print-numbers ((int n))
  (dotimes i n
    (printf "%d\n" i)))
;==EXPECTED==
#include "stdio.h"

void print_u002D_numbers(int n)
{
  for (int i = 0; i < n; i++)
    {
      printf("%d\n", i);
    }
}
