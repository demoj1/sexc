(struct Point :fields (int x) (int y))

(defn int main ()
  (decl (Point p) (Point# 1 2))
  (return 0))
;==EXPECTED-ERROR==
<stdin>:3:1: error[compile]: Point# designated init expects pairs like (field value)
(defn int main ()
^
