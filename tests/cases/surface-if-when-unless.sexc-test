(defn int classify ((int x))
  (when (> x 100)
    (return 1))
  (unless (< x -100)
    (return 0))
  (return -1))
;==EXPECTED==
int classify(int x)
{
  if (x > 100)
    {
      return 1;
    }
  if (!(x < -100))
    {
      return 0;
    }
  return -1;
}
