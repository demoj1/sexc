(include "stdio.h")

(defn int gcd ((int a b))
  (while (not= b 0)
    (decl (int t) b)
    (set b (% a b))
    (set a t))
  (return a))

(defn int main ()
  (printf "GCD: %d\n" (gcd 48 18))
  (return 0))
