; Small helper macros for raylib-oriented examples

(%defmacro begin-frame ()
  `(BeginDrawing))

(%defmacro end-frame ()
  `(EndDrawing))

(%defmacro clear (color)
  `(ClearBackground ,color))

(%defmacro circle (pos r color)
  `(DrawCircleV ,pos ,r ,color))
